[project]
name = "mqtt-systemd-controller"
version = "0.1.0"
description = "MQTT-based systemd service controller with polling and race condition handling"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "paho-mqtt>=2.1.0",
]
